# Всё под public/ отдаётся байт-в-байт: и конверт каталога доверия
# (tsl1.<payload>.<sig> — одна строка base64url), и манифест обновлений
# проверяются по подписи над телом ответа целиком. Любой \r, подставленный
# при выкладке, ломает проверку подписи у клиента, поэтому конвертация
# концов строк здесь запрещена.
public/** -text
